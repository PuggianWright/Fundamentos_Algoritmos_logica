console.log(19.9 * 0.6);

let preco = 19.90;
let desconto = 0.4;
let precoComDesconto = preco * (1 - desconto);
console.log(precoComDesconto);    

let nome = "Caderno" //String (Texto) -> sequencia de símbolos
let categoria = "Papelaria"
console.log("Produto: " + nome 
    +", Categoria: " + categoria    
    + ", Preço:" + preco
    + ", Desconto:" + desconto);

    // Anotações:
//matemática apenas, igual no papel;
// O bom é organizar e por em ordem variaveis e literais;
// Escolher bons nomes para as variaveis;
// virgula pode ser usada como espaço+variavel;