const primeiroTexto = 'C';
const segundoTexto = 'D';
const terceiroTexto = 'D';
const quartoTexto = 'A';
const quintoTexto = 'D';
const sextoTexto = 'C';
const setimoTexto = 'B';
const oitavoTexto = 'R';

let texto1
let texto2
let texto3
let texto4
let texto5
let texto6
let texto7
let texto8
{ // PrimeiroTexto
if (primeiroTexto == 'A') {
    texto1 = 'Neste primero excelente conceito...'
} else if (primeiroTexto == 'B') {
    texto1 = 'O conceito bem aplicado apresentado...'
} else if (primeiroTexto == 'C') {
    texto1 = 'Tal conceito medianamente visto...'
} else if (primeiroTexto == 'D') {
    texto1 = 'Este banal conceito...'
}
}

{ // SegundoTexto
if (segundoTexto == 'A') {
    texto2 = 'Neste primero excelente conceito...'
} else if (segundoTexto == 'B') {
    texto2 = 'O conceito bem aplicado apresentado...'
} else if (segundoTexto == 'C') {
    texto2 = 'Tal conceito medianamente visto...'
} else if (segundoTexto == 'D') {
    texto2 = 'Este banal conceito...'
}
}

{
if (terceiroTexto == 'A') {
    texto3 = 'Neste primero excelente conceito...'
} else if (terceiroTexto == 'B') {
    texto3 = 'O conceito bem aplicado apresentado...'
} else if (terceiroTexto == 'C') {
    texto3 = 'Tal conceito medianamente visto...'
} else if (terceiroTexto == 'D') {
    texto3 = 'Este banal conceito...'
}
}

console.log
(
    texto1 , 
    ('\n') + texto2 , 
    ('\n') + texto3 , 
    ('\n') + texto4 , 
    ('\n') + texto5 , 
    ('\n') + texto6 , 
    ('\n') + texto7 , 
    ('\n') + texto8
    )